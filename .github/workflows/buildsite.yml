name: Build site
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v1
      - name: Get updated files
        shell: bash
        run: |
          pip install -r requirements.txt
          make html
